@model IEnumerable<News_MVC.Models.ArticleTags>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Assign Tag to Article", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Articles.ArticleName)
         </th>
        <th>
            @Html.DisplayNameFor(model => model.Tags.TagName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @*@Html.DisplayFor(modelItem => item.Articles.ArticleName)*@
            @Html.ActionLink(item.Articles.ArticleName, "ArticleContent", "Articles", new { id = item.Articles.ArticleID }, null)

        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.Tags.TagName)*@
            @Html.ActionLink(item.Tags.TagName, "TagArticleList", "ArticleTags", new { id = item.TagID }, null)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.Id }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
